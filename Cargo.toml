[package]
name = "bullet"
version = "0.1.0"
edition = "2021"
description = "BulletScan: RustScan-powered port discovery followed by Nmap enumeration"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
regex = "1"
